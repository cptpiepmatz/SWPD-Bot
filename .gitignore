#ignore ide data
.idea/

#ignore npm packages
node_modules/

#ignore compiled files
out/

#don't store the login token in the repo
.token

#don't store the repo the git client pulled
repo/

#don't store what the BBClient stored as cache for stable reboots
.bb-pr-data
